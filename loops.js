function forLoop() {
  const forLoop = [];
  const t = 25;

  for (let i = 0; i < t; i++) {
    console.log(`"I am ${i} strange loop."`);
  }

  return [array, t];
}
function whileLoop() {
  let countdown = 100;
  while (countdown > ) {
    console.log(countdown);
    countdown--;
    return 'done';
  }
}
